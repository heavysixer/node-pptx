let { Text } = require('./text');

class Shape extends Text {
    // TODO... a shape object in PowerPoint is virtually the same as text, so we'll just inherit Text and build off of it...
}

module.exports.Shape = Shape;
